function loadXMLDoc()
{
    alert("In Function");
    /*var xmlhttp;

    if(window.XMLHttpRequest)
    {
        xmlhttp=new XMLHttpRequest();

        var pkgname=document.getElementById("newPkgName").value;
        alert(pkgname);

        var pkgvalidity=document.getElementById("newPkgValidity").value;
        alert(pkgvalidity);
    }
    xmlhttp.onreadystatechange=function()
    {
        if(xmlhttp.readystate==4 && xmlhttp.status==200)
        {
            document.getElementById("txtPrice").innerHTML=xmlhttp.responseText;

        }
    }
    xmlhttp.open("GET","/chngpkg/pkgprice?q="+pkgname,true);
    xmlhttp.send();*/
}
